{% extends 'app/layout.html.twig' %}

{% block content %}
    <div class ="row">
        <div class = "col-xs-6">
            {{ google_map_container(map) }}
            {{ google_map_css(map) }}
            {{ google_map_js(map) }}
        </div>
    </div>
{% endblock %}